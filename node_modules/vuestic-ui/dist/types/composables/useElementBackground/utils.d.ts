export type ColorArray = [number, number, number, number];
export declare const parseRGBA: (color: string) => ColorArray | undefined;
/**
 * Super slow function. Must be covered with cache as much as possible.
 * It actually goes through all CSS and search for value.
 * This is needed to prevent transition delay when changing background color.
 */
export declare const getElementBackgroundFromCSS: (el: HTMLElement) => string | undefined;
export declare const getElementBackground: (element: HTMLElement) => string | undefined;
