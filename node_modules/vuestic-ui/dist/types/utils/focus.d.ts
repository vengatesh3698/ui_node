/**
 * Since `.focus()` does not dispatch native event, this method will dispatch it automatically
 * @emits focus
 */
export declare const focusElement: <T extends HTMLElement | undefined>(el: T) => void;
/**
 * Since `.blur()` does not dispatch native event, this method will dispatch it automatically
 * @emits blur
 */
export declare const blurElement: <T extends HTMLElement | undefined>(el: T) => void;
